"""
遍历：
- 都支持for遍历
- 列表[]、元组()、字符串str, 支持while循环，集合set、字典dict不支持（无法下标索引）

统计功能：
- len(容器）
- max(容器）
- min(容器)

数据转换
- list(容器)
- set(容器)
- tuple(容器)
- str(容器)

排序
- sorted(容器)
"""

# 字符串比较，一位一位的比较，谁的ASCII大则大
print('c' > 'ab')  # true
